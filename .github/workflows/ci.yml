name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Python dependencies
        run: python -m pip install ".[core,api,dev]"
      - name: Run Python tests
        run: python -m pytest -q
      - name: Smoke-test review backfill CLI
        run: python scripts/backfill_reviews.py --help
      - name: Run Ruff
        run: ruff check .

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install data-build dependencies
        run: python -m pip install ".[core]"
      - name: Install web dependencies
        working-directory: web
        run: npm ci
      - name: Rebuild public data
        working-directory: web
        run: npm run build:data
      - name: Verify committed public data is current
        run: git diff --exit-code -- web/public/data.json
      - name: Run web tests
        working-directory: web
        run: npm test
      - name: Build static site
        working-directory: web
        run: npm run build
